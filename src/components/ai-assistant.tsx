
"use client";

// This component has been removed as per user request.
// Returning null to prevent breaking changes in pages that might still import it.
export default function AIHelper() {
  return null;
}
